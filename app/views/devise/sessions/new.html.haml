-content_for :navbar_class do
  bg-transparent fixed midgray sm-col-12
#signin.pt4
  .max-width-3.mx-auto.mt4
    .bg-lightgray.rounded.p3.mt4.max-width-2.mx-auto
      .bg-blue.block.mx-auto{style: 'width: 100px; height: 100px; border-radius: 100px;'}
        = image_tag "penguin-white.svg", width: '100px', style: 'margin-top: 10px;'
      %h1.center.domino-gray Log In To Your Account
      =simple_form_for resource, as: resource_name, url: session_path(resource_name) do |f|
        =#render 'shared/error_messages', object: f.object
        - flash.each do |name, msg|
          = content_tag :div, msg, :id => "flash_#{name}", style: 'text-align: center', class: 'domino-gray' if msg.is_a?(String) 
        .form-inputs.max-width-1.mx-auto
          %label.domino-gray Email Address
          = f.input_field :email, required: false, autofocus: true, class: 'mb2 mt1', style: 'height: 40px;'
          %label.domino-gray Password
          = f.input_field :password, required: false, class: 'mt1', style: 'height: 40px;'
          .my2
            %button.btn-remember-me{ type: "button", data: {target: "user_remember_me"}, style: "width: 25px;height: 25px; border-radius: 3px; border: solid 2px #000000; position:relative; background-color: white"}
              %i.fa.fa-check.white{style: "position:absolute;bottom:2px;right:2px;display: none"} 
            %label.domino-gray{style: 'font-weight:100;'} Keep me logged in
          .display-none
            = f.input_field :remember_me, class:'h1', as: :boolean, label: false if devise_mapping.rememberable?
          = f.button :submit, "LOG IN", class: "bg-blue white py2", style: 'border-radius:40px; border:none;height:50px; color:white;'
          .mt2.center
            - if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
              = link_to "Forgot my password", new_password_path(resource_name), style: 'color: black;font-weight:100;', class: 'domino-gray'
    %p.center.domino-gray
      Dont have an account?
      %span.blue
        = link_to "Get Started", root_path, class: 'text-decoration-none blue'
:javascript
  $(document).ready(function(){
    $('#navbar, #navbar-brand, #link-login').css('color', '#666666');

    $('.btn-remember-me').on('click', function(){
      if($(this).hasClass('active')){
        $(this).css('background-color', 'white');
        $(this).css('border-color', 'black');
        $('i', this).css('display', 'none');
        $(this).removeClass('active');
      }else{
        $(this).css('background-color', '#00CCFF');
        $(this).css('border-color', '#00CCFF');
        $('i', this).css('display', 'inline');
        $(this).addClass('active');
      }
      var target = '#' + $(this).data('target');
      $(target).prop('checked', function( i, val ) {
        return !val;
      });
    });
  });