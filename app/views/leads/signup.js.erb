<%- if @lead.errors.any? %>
  $('.errors').html('<%= j(render 'errors') %>');
<%- else %>
  $('.errors').html('');
  $( '.reserve').addClass( 'submitted' );
  $( 'input, button' ).prop('disabled', true);
  $( '.reserve button')
      .text('COMPLETED');
  //Track conversions to GA, Heap, FB, and anyone else we need.
  track_lead_creation();
<%- end %>