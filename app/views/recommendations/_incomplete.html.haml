.sm-col.sm-col-12.md-col-4.p2
  .recommendation.border-light.p2.py3.relative.rounded-20
    -if current_user.role == 'concierge'
      =link_to recommendation_path(recommendation), method: 'DELETE', data: {confirm: 'Are you sure?'}, class: 'absolute rounded bg-gray p1 white right-10 top-10 lh1' do
        .fa.fa-times
    =render recommendation.recommendable, recommendation: recommendation
-if(recommendation.recommendable_type == 'Product')
  =render 'products/product_modal', recommendation: recommendation, product: recommendation.recommendable
-if(recommendation.recommendable_type == 'Task')
  =render 'tasks/task_modal', recommendation: recommendation, task: recommendation.recommendable